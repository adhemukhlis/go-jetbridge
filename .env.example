API_KEY="my-secret-api"

# WARNING: Avoid using remote database connections during local development.
# This prevents the risk of accidentally resetting or dropping the configured database.
DATABASE_URL="postgresql://db_user:db_pass@db_host:db_port/db_name?sslmode=disable"